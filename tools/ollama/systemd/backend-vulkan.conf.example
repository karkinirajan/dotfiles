# Alternate backend.conf content for Vulkan — kept for reference/rollback.
# NOT currently active: see claude-code-bench/ollama-claude-setup.md §2 for
# why ROCm is live instead (a reproduced Vulkan crash at large prompt sizes).
# Written by `ollama-backend vulkan`.
[Service]
Environment="OLLAMA_LLM_LIBRARY=vulkan"
