# Intentionally minimal. The previous contents were:
#   [Autologin]
#   Session=plasma
# a leftover from the KDE/Plasma install that was removed (see
# dotfiles wm/hyprland/KDE-REMOVAL.md). Plasma is no longer installed, so that
# pointed SDDM at a session that does not exist.
# Hyprland-specific settings live in /etc/sddm.conf.d/hyprland.conf.
